"""Bare-metal bootstrap scripts, shipped as package data.

The ``osprey-bootstrap`` console script (see :mod:`osprey.bootstrap`) locates
and runs ``bootstrap.sh`` from this directory to reproduce the DeepStream 8.0
image build on a fresh Ubuntu 24.04 host — no Docker.
"""
